abstract class PropertiesTeamRepo {
  int doubleScore({ required List scores});
  int tripleScore({required List scores});
  int maximumScore({ required List scores});
  int freeScore({required List scores});
}